# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

> The line above is not decorative. This repo runs **Next.js 16 + React 19** with breaking changes from older versions. Before writing routing, metadata, data-fetching, or `<Image>` code, read the relevant guide in `node_modules/next/dist/docs/`. `params` are async (`Promise`), `generateMetadata`/page components `await` them, and `images.qualities` must allow-list every `quality` value used.

## Commands

```bash
npm run dev      # next dev -H 0.0.0.0 (binds all interfaces so a phone on the LAN can load it)
npm run build    # next build — also the real type/route check; run before considering work done
npm run start    # serve the production build
npm run lint     # eslint (flat config, eslint.config.mjs)
```

There is no test suite. `npm run build` is the closest thing to a correctness gate — it fails on type errors, bad routes, and disallowed image qualities. Import alias is `@/*` → repo root (e.g. `@/lib/data/...`, `@/components/...`).

## Architecture

This is a marketing/SEO site for a Perth garage-door business. Almost every page is **data-driven** — from local TypeScript content by default, or from the **live CMS** where its cutover flags are on (see **CMS** below) — rendered through a small set of reusable template components. Public pages are statically generated at build time (`generateStaticParams` + `dynamicParams = false`) with ISR revalidation; there is no per-request fetching for them (the CMS, when enabled, is read at build/revalidate, not per request). The `/admin/*` CMS dashboard is the exception — it is dynamic/server-rendered.

### The content pipeline (the core pattern)

Each page *type* flows through four layers — to add or change content you usually touch only the first:

1. **`content/<type>/...`** — the content store. Two shapes exist, both ending in a **registry** that exports an array:
   - **Directory-per-type** (one file per instance): `content/<type>/<slug>.ts` is a plain typed object; the sibling `content/<type>/index.ts` imports each instance and exports them as an array. Used by the larger page types — `articles`, `case-studies`, `comparison-pages`, `cost-guides`, `landing-pages`, `service-pages`.
   - **Single-file registry**: a flat `content/<type>.ts` exporting the array directly. Used for the smaller/list-style datasets — `services.ts`, `problems.ts`, `reviews.ts`, `testimonials.ts`, `gallery.ts`, `service-areas.ts`, `service-area-regions.ts`, `service-suburb-pages.ts`, `blog.ts`.
   - Ship a new page by adding the entry (and pushing onto the registry array) — no routing or component changes.
2. **`lib/data/<type>.ts`** — an `async` data-access layer (`get…BySlug`, `get…Slugs`, `get…`) wrapping the registry. There is one per content type (see `lib/data/` for the full list). **All call sites (routes, templates, sitemap) go through this layer, never the `content/` registries directly.** It is deliberately async so the backing store swaps from local files to the CMS API without changing call sites — a cutover that is **now live** behind per-type flags (see **CMS** below; original design in `docs/cms-architecture.md`).
3. **`types/<type>.ts`** — the interface shared by the content file, the data layer, and the template. `types/index.ts` holds the cross-cutting ones (`FAQ`, `Service`, `Problem`, `ServiceArea`, `BreadcrumbItem`).
4. **`components/.../<type>-page-template.tsx`** — the `…PageTemplate` component that consumes one typed object and renders the page from composable section components in `components/sections/<type>/` (or `components/page/` for the service-suburb template).

### Routing

Most routes are thin: resolve slug → call the data layer → render the template, plus `generateMetadata` using `buildMetadata`.

- **`app/[slug]/page.tsx` is shared by four "flat" page types** — service, comparison, cost-guide, and service-suburb pages all live in one top-level slug namespace and are resolved by trying each registry in turn (in that order; the first match wins, so slugs must be unique across all four). `generateMetadata` and the default export each walk the same chain. **Never add a sibling `app/[other]/page.tsx`** — Next.js rejects two differently-named dynamic segments at the same path level (ambiguous-route build error). Add a new flat type by extending this file's resolution chain *and* its `generateStaticParams` `Set`.
- Other types have their own namespaced dynamic routes: `app/blog/[slug]`, `app/case-studies/[slug]`, `app/problems/[slug]`, `app/lp/[slug]`.
- `app/lp/*` are **paid landing pages**: conversion-first, `noindex`, never linked in-site, and they use their own minimal chrome from `app/lp/layout.tsx`.
- **Static listing / standalone pages** (their own folder, no dynamic segment): `app/page.tsx` (home), and the index pages `app/services`, `app/service-areas`, `app/cost-guides`, `app/blog`, `app/case-studies`, `app/problems`, `app/reviews`, `app/gallery`. Plus standalone pages `app/about`, `app/contact`, `app/calculator`, `app/privacy`, `app/terms`, `app/warranty`, `app/warranty-registration`, and `app/garage-door-motors-perth` (branded Capital 1100N/1500N motor product page — sections in `components/sections/motors/` with a shared `motor-data.ts`; imagery lives on the Bunny CDN under `capital-garage-door/motors/`, re-uploadable via `npx tsx scripts/upload-motor-images.ts`; the slug is reserved — a CMS page published under it would be shadowed by this route). These pull lists from the data layer but aren't part of the slug-resolution chain. `app/service-areas` is a single listing page (regions + suburbs) — there is **no** `app/service-areas/[slug]` route; individual suburb pages are flat pages served by `app/[slug]`.

### Site chrome & layout

`app/layout.tsx` (root) loads fonts, the site-wide JSON-LD `@graph` (business + WebSite, see SEO below), and wraps everything in `LazyMotionProvider` → `SiteChrome`. **`components/layout/site-chrome.tsx` is a client component that branches on `usePathname()`**: `/lp/*` routes get bare chrome (the landing layout supplies header/footer); every other route gets the full header, footer, smooth-scroll, scroll progress, and the welcome intro loader. Header nav links use `HoverPrefetchLink` (`components/ui/hover-prefetch-link.tsx`) — prefetch on hover/touch intent only, because the 9 always-in-viewport nav links otherwise fire ~1 MB of route prefetch at load; footer links are `prefetch={false}`. GA4 (gtag) loads via `components/analytics/deferred-analytics.tsx` on `requestIdleCallback`, not `afterInteractive`.

### SEO

> **Before creating or substantially editing any page, read [`on-page-seo.md`](on-page-seo.md) and satisfy every applicable item.** It is the canonical on-page-SEO checklist (15 categories / 80+ items) tailored to this repo's helpers + content pipeline, with a per-page-type required-schema matrix and a "definition of done". Every page-generation task starts there. **§0 of that file covers live SEO data (GSC, GA4, DataForSEO are connected and verified — see below) to pull real keyword/ranking/traffic numbers instead of guessing.**

- **Live SEO data sources are connected**: Google Search Console (property `https://capitalgaragedoors.com.au/`), GA4 (`properties/544287277`), and DataForSEO (metered, low balance — check before bulk calls) via service-account/API credentials in `~/.config/claude-seo/` (outside the repo). Scripts in `C:\Users\Mohammad swedan\.claude\skills\seo\scripts\`, run with `py -3.12`. Full usage + commands in `on-page-seo.md` §0. PSI/CrUX (Core Web Vitals) are not yet connected (need a separate plain API key).
- `config/site.ts` — single source of truth for NAP (name/address/phone), nav, hours, social, URLs, legal entity (`legalName` — no invented corporate suffix; an audit flagged a bogus "LLC") and `abn` (TODO: fill with the real ABN — renders in the footer + schema `taxID` once set). **Hours in prose must use `formatHoursSummary()` (`lib/utils.ts`)**, never hand-written strings — the Contact page once showed a third conflicting hour set.
- **Review figures have ONE source: `getReviewsSummary()`** (CMS reviews). The site-wide schema `aggregateRating`, the hero's rating widget (passed as a prop from `app/page.tsx`), and `/reviews` all read it. Never hardcode a rating/count anywhere visible — schema-vs-visible divergence is a Google structured-data policy violation (the audit's #1 finding).
- `lib/seo/metadata.ts` — `buildMetadata({ title, description, path, image?, noIndex?, publishedTime?, lastModified? })` produces a `Metadata` object with canonical/OG/Twitter defaults. Use it for every page's `generateMetadata`. It applies `title.absolute` (the root `"%s | Capital Garage Doors"` template is **not** appended — author titles complete and ≤60 chars), emits the default 1200×630 OG card with dimensions+alt unless a page-specific `image` is passed, and `console.warn`s in dev when title >60 / description >160 chars.
- `lib/seo/schema.ts` — JSON-LD builders rendered via `components/seo/json-ld.tsx`. The site-wide entity is ONE `@graph` (`siteGraphSchema()` in `app/layout.tsx`): HomeAndConstructionBusiness (`@id` `…/#business`, GeoCircle areaServed, hasMap) + WebSite. Per-page nodes reference the business by `BUSINESS_ID` — never emit a second standalone Organization/LocalBusiness for the same entity.
- `app/sitemap.ts` (real `lastmod` on every URL — hubs inherit their newest child's date; no `changefreq`/`priority`, Google ignores them), `app/robots.ts`, `app/manifest.ts`, `app/llms.txt/route.ts` (AI-assistant site index per llmstxt.org, built from the data layer) are generated from the data layer + site config.
- Security headers (HSTS, nosniff, X-Frame-Options, Referrer-Policy, Permissions-Policy) + `poweredByHeader: false` are set in `next.config.ts` `headers()`. No CSP yet (needs an inline-script inventory first).
- **`scripts/sync-seo-fixes.ts`** — re-runnable CMS content-fix sync (titles ≤60, broken-link repoints, de-orphan links, pricing pins, hero-asset fixes). Defaults to the LOCAL CMS; run against production only with explicit `CMS_API_URL=https://cgd.runasp.net CMS_ADMIN_PASSWORD=…`. Gotcha it encodes: article `RelatedArticles` links are slug-ified and re-prefixed `/blog/` by `map-article.ts` — a non-blog URL in that group renders as a phantom link; use `RelatedServices` (pass-through hrefs) instead. `scripts/import-new-service-pages.ts` and `scripts/import-new-problem-pages.ts` are the same-pattern importers that created the roller-installation + sectional-doors service pages and the 7 problem pages WITH pricing/review pins attached (the bulk importer leaves pins empty). `scripts/import-door-type-pages.ts` (same pattern + an append-if-missing related-links step on existing pages) created the four door-type pages — `/roller-doors-perth`, `/tilt-garage-doors-perth`, `/custom-garage-doors-perth`, `/commercial-roller-doors-perth` — completing the type × segment matrix with the existing sectional + commercial-hub pages; their heroes live on the Bunny CDN under `capital-garage-door/door-types/`, re-uploadable via `npx tsx scripts/upload-door-type-images.ts` (which deliberately skips the one source render showing a competitor-branded shirt). `scripts/import-new-cost-guide-pages.ts` (same pattern) created the three cost-guide pages — `/garage-door-spring-replacement-cost-perth`, `/garage-door-service-cost-perth`, `/garage-door-motor-replacement-cost-perth` — with each cost-table row pinned to a `PricingItems` scenario (enriching the not-yet-enriched `includes/costFactors/nextStep` columns, mirroring `sync-seo-fixes.ts`) plus repair-page cross-links. `scripts/import-service-suburb-pages.ts` (same pattern, no pins — suburb pages carry none) created the four repairs-suburb pages (Gosnells, Cannington, Lathlain, Southern River) with per-slug Published/Draft status. `scripts/import-case-studies-from-jobs.ts` (same pattern) created 20 real case studies from the gallery job photos — crucially it stores `data.images` as `{ src: <CDN URL>, alt, caption }` (NOT `{ assetId }` like the original `lib/cms/import/case-studies.ts`), because the CMS resolve blob round-trips verbatim and `map-case-study-page.ts` reads `img.src`; storing assetIds yields empty `src` and no photo. These feed the suburb pages' **"Recent work"** section (`components/page/recent-work.tsx`, replacing the old placeholder `local-proof.tsx`): `getCaseStudiesForSuburbPage()` (`lib/data/case-studies.ts`) resolves case studies by the page's hand-picked `caseStudySlugs` else auto-matches by `suburb`, keeps only those with a real (http) photo, and the section renders nothing when empty. Cards show before-only/after-only/both (derived from image captions) and link to `/case-studies/{slug}`; real photos also now render on the case-study detail page's `BeforeAfterGallery` via the shared `components/page/case-study-image.tsx`. `scripts/enhance-installation-page.ts` is the July-2026 GSC-surge enhancement of `/garage-door-installation-perth` (title/copy/FAQs for the "replacement" + "installers" clusters, cost-table pricing pins, motors/door-type related links, suburb-grid additions) — it also published the last two suburb drafts and repointed the Gosnells⇄Southern River nearby-suburb chips at each other; its seoTitle/seoDescription desired state is deliberately duplicated in `sync-seo-fixes.ts`'s `SEO_FIXES`, keep the two in lockstep. `scripts/import-garage-doors-perth-page.ts` (same pattern) created **`/garage-doors-perth`**, the parent product/buying hub for the whole door range — the 2026-08-02 GSC pull found the "garage doors perth" buy cluster pulling 1,641 impressions / 2 clicks across 68 queries at average position 30–58 with no page targeting it (the four door-TYPE pages existed but had no parent for the head term), and a live Perth SERP confirmed the top 10 is entirely supply-and-install hubs of that shape. It is also the **new 301 target for `/services/garage-doors`** (see the redirects bullet below). The importer adds upward `RelatedServices` links from the four type pages plus the installation, commercial and roller-install pages, and it filters `routeGroup === "Flat"` on every slug lookup. Remember: `app/[slug]` now sets `dynamicParams = true` (on-demand rendering), so a page created in the CMS resolves on the live site **without** waiting for a deploy — it's only added to `generateStaticParams`/prerendered at the next build; `app/problems/[slug]` follows the same default.

- **`HREF_FIXES` in `sync-seo-fixes.ts` must never point at a URL that redirects.** A rule written in July mapped `/garage-door-motor-replacement-cost-perth` → `/blog/garage-door-motor-replacement-cost-perth`, which was correct when the flat URL 404'd — but the July recovery work reversed that (the blog copy was unpublished and 301'd to the flat page, and `import-new-cost-guide-pages.ts` created the flat cost guide). Every subsequent run therefore rewrote good internal links into a redirecting URL, and the flat page ended the 28 days to 2026-08-01 with **zero impressions** while the 301'd blog URL still earned 134 imp / 5 clicks at position 15.5. Reversed on 2026-08-02 (it now repairs 4 pages); **do not flip it back**. When adding any `HREF_FIXES` entry, curl the destination first — a 200, not a 3xx.

- **Legacy-URL redirects (`next.config.ts` `redirects()`) are load-bearing SEO, not tidiness.** The old pre-2026-07-06 site's URLs still hold indexed impressions. The 2026-07-24 GSC export (3 months to 07-22: 228 clicks / 18,602 impressions / avg position 28.3) found ~5,600 impressions a quarter landing on 404s and redirect→404 chains — `/services/garage-doors` (2,322 impressions, the #2 URL site-wide), `/blogs/garage-door-springs-guide` (1,834 at position 10.7, the best-ranking substantial page), `/emergency-service`, `/new-installations`, `/commercial-services`, `/maintenance-service`, `/book-an-appointment` (position 7.35), `/request-a-quote`, plus three case studies deleted in `1842de8`. All are now 301'd. **Order matters: specific rules MUST precede the `/blogs/:slug*` catch-all**, and a rule is only worth adding if its destination returns 200 — check before shipping, since `/blogs/garage-door-springs-guide` spent a week 301-ing into a 404. Two other findings from the same export: an apparent **54-day index blackout** (2026-05-05 → 06-27, zero impressions) — **DISPROVEN 2026-08-02, do not repeat it as fact**: a Domain property (`sc-domain:capitalgaragedoors.com.au`) was verified that day and shows *continuous* daily data across the whole window (121 unbroken days from 04-01, 2,600–3,700 impressions every week). The gap existed only in the URL-prefix property, i.e. it was a **reporting/verification artifact of that property, not a loss of traffic**. Always cross-check a suspected traffic collapse against both properties before acting on it — and the homepage outranking `/garage-door-repairs-perth` for the site's #1 term 4,937 impressions to 7. **2026-08-02 update:** `/services/garage-doors` was retargeted from `/services` to the new `/garage-doors-perth` hub — it is still the #4 URL site-wide (1,525 impressions in 28 days) and still ranks *above* the real pages for `roller doors perth` (29.4) and `residential garage doors perth` (31.1), so handing a product-intent URL's equity to a nav listing was waste. **Two GSC-measurement traps that produced wrong numbers in that same session — avoid both:** (1) query-dimension totals EXCLUDE anonymised long-tail queries, so they run far below page/date-dimension totals for the same window (28 days to 08-01: 115 clicks by query dim vs **248** by page dim) — never compare one dimension's total against another's; (2) the URL-prefix property under-reports historical windows (see the disproven blackout above), so trend comparisons must use `sc-domain:`. Corrected trend, date dim, Domain property: Jun 07–Jul 04 = **86 clicks / 12,395 impressions**, Jul 05–Aug 01 = **231 clicks / 17,929 impressions** (26 days of data — GSC lags ~2 days). Real gain ≈ 2.7× clicks / +45% impressions, but the weekly series shows a launch spike (99 and 72 clicks the weeks of 07-06 and 07-13) decaying to 29 the week of 07-20 — the aggregate flatters it, and site-wide CTR is back at ~0.6%.
- **One slug, one route group.** `garage-door-motor-replacement-cost-perth` existed in BOTH `flat` and `blog`, and the two quoted *different prices* for the same job — the blog copy said $450–$1,200 for a motor replacement against the price list's $770–$990. The flat cost-guide copy (pinned to `PricingItems`) is canonical; the blog copy is unpublished and 301'd. **Blog posts must never state a price that isn't taken from `pricing-data.ts` / CMS `PricingItems`.** Seven other posts quoted invented, mutually contradictory figures below the real list (springs across three posts said $150–$450 against the real $240–$280 single / $440–$550 pair; the buying guide priced a new door at $1,500–$2,500 against the real $3,000–$5,000). `scripts/fix-blog-pricing.ts` corrects all 42 statements — exact-substring replacement over the serialised `data` + `faqs`, so it's path-independent and idempotent. **Run `--check` first**: it verifies every `from` string still exists verbatim via the anonymous public resolve endpoint (no credentials), because an edited upstream string makes the replacement silently no-op and leaves a wrong price live. `scripts/import-springs-guide.ts` is the Phase-1 recovery script (recreates the spring pillar at `blog/garage-door-springs-guide` from `content/articles/garage-door-springs-guide.ts`, cross-links it from the two spring money pages, and retires the duplicate); its article carries the correct ranges and is the reference for what corrected pricing copy looks like.
- **A new suburb page is invisible until its suburb chip is linked.** `/service-areas` renders a suburb as a plain chip unless the CMS suburb row carries a `pageId` → the public DTO's `pageHref` (`lib/data/service-area-regions.ts` `mapRegion()`); that chip is the *only* inbound link most suburb pages ever get. `scripts/import-service-suburb-pages.ts` created Gosnells, Cannington, Lathlain and Southern River but never set it, so a 2026-07-24 crawl of all 93 sitemap URLs found **Cannington and Lathlain with zero inbound internal links, and Gosnells⇄Southern River linking only to each other** — a closed island, made worse by `enhance-installation-page.ts` repointing their nearby-suburb chips at each other. Only 15 of 342 CMS suburbs have `pageHref` set. GSC shows the cost: linked suburb pages sit at positions 4.0–6.0 (Clarkson, Midland, Thornlie, Scarborough) while these four sit at 13.8–68.5. **Any script that creates a `ServiceSuburbPage` must also set the matching suburb's `pageId`**, or fix it after via `/admin/service-areas` → edit suburb → "Linked page ID". `scripts/link-suburb-pages.ts` is the re-runnable fixer (sets `pageId` on the four July suburbs; idempotent). **`scripts/enhance-suburb-pages.ts` (2026-08-01, run against prod) is the money-keyword enhancement of all 19 suburb pages**, built from live GSC/DataForSEO/SERP research (volumes: baldivis 110/mo, rockingham 90–110, joondalup 90, canning vale 50; CPCs $27–$103; the homepage was outranking suburb pages for their own "garage door repairs {suburb}" terms). It applies desired-state: Same-Day titles/descriptions (the baldivis/success/midland titles stay pinned by `sync-seo-fixes.ts` — untouched), one unique local paragraph per suburb naming GSC-demand localities (Huntingdale, Wangara, Safety Bay, Aveley…) + the brands list (its `PARA_MARKER` "B&D, Steel-Line, Centurion" is the idempotency guard — don't reuse that phrase in other intro paragraphs), 3 appended FAQs (brands / new-door installs / most-common-problem PAA with a coastal variant), placeholder NearbySuburbs-chip repoints (only chips whose `staticHref` is `/service-areas`), `caseStudySlugs` picks for the 6 suburbs with no same-suburb job photos, and hub-grid completion on `/garage-door-repairs-perth`. Gotcha it encodes: **admin page slugs are only unique per route group** (`garage-door-repairs-perth` exists as both Flat and Lp) — slug lookups must filter `routeGroup === "Flat"`. The paired frontend change: `ServicePageTemplate`/`ServiceAreaGrid` take an optional `areaLinks` map (lowercased suburb name → href) and `app/[slug]/page.tsx` builds it from `getServiceSuburbPageSlugs()`, so every service page's "Areas We Service" chip that matches a live suburb page renders as a real link (the hub→spoke internal-link fix; chips stay plain text in the admin live-editor, which passes no `areaLinks`). **Batch 2 (2026-08-01): 15 more research-picked suburb pages exist in the prod CMS as DRAFTS** — Mandurah (420/mo, the biggest keyword; region text "Mandurah, WA"), Bayswater, Willetton, Belmont, Malaga, Stirling, Osborne Park, High Wycombe, Port Kennedy, Maddington, Huntingdale, Kingsley, Riverton, Duncraig, Kalamunda (Cockburn and Victoria Park deliberately skipped — the Success and Lathlain pages target them). Content lives in `content/service-suburb-pages-batch2.ts` (a `makeSuburbPage()` builder + unique per-suburb copy, spread into the main registry); `scripts/import-suburb-pages-batch2.ts` created them (409 = skip). **Batch 3 (2026-08) added 15 more drafts across three southern corridors** — Canning River (Shelley, Rossmoyne, Parkwood, Langford, Lynwood, Ferndale, Wilson), southern growth (Harrisdale, Piara Waters, Forrestdale) and Armadale (Kelmscott, Camillo, Champion Lakes, Seville Grove, Brookdale). Content is `content/service-suburb-pages-batch3.ts` (a richer `makeSuburbPageV2` builder: 5 bespoke intro paragraphs + 10–11 FAQs per page, with `flags` — `riverside`/`estate`/`commercial`/`olderStock`/`rentals`/`boats` — varying the service, problem and trust cards so no two pages read alike); created by `scripts/import-suburb-pages-batch3.ts`, wired in afterwards by `scripts/finalize-suburb-pages-batch3.ts` (same contract as the batch-2 finalizer, own `REGION_ANCHOR`). **These pages introduced guide-price tables on suburb pages**: `CostGuidance` gained an optional `rows`, `map-service-suburb-page.ts` maps `dto.pricingRows` (reusing `map-service-page.ts`'s `priceLabel()` formatting), and `components/page/cost-guidance.tsx` renders a crawlable `<table>` when rows exist — driven by research showing **every one of the 15 live SERPs surfaces "{suburb} cost/prices/price list" in its related searches**. Pins are catalog scenarios matched by exact string (`PRICING_PINS` in the importer — note the `×` in "Broken springs (×2)"); the importer hard-fails if any `$` figure appears in the hand-written copy. **Existing suburb pages have no pins, so they render exactly as before — adding pins to them is an easy follow-up.** **`/garage-door-repairs-cockburn-central` is a separate, deeper draft** (`content/service-suburb-pages-cockburn-central.ts` — hand-written, not the batch-2 builder: 5 local paragraphs, 11 FAQs, a commercial section for the Bibra Lake/Jandakot belt, `Building2` added to `components/page/icons.ts`). It was built to beat an exact-match-domain competitor whose page has zero FAQs, and it **takes over the Cockburn terms the Success page used to claim** — GSC showed Success capturing none of them (the homepage ranked at position 49.8). `scripts/import-cockburn-central-page.ts` created it as a Draft; its **Phase B runs only once the page is Published** and then narrows the live Success page, links the existing "Cockburn Central" suburb row, repoints chips and extends the hub grid. **The Success narrowing is desired state in three places that must stay in lockstep — `sync-seo-fixes.ts` (pinned title, now "Garage Door Repairs Success | Same-Day Local Service"), `enhance-suburb-pages.ts` (description + marker paragraph) and this script — never restore "& Cockburn" to any of them.** **After the user publishes any of them from /admin, run `scripts/finalize-suburb-pages-batch2.ts`** — it wires ONLY the published ones (re-runnable as publishing happens in stages): suburb-row `pageId` (creating missing rows, e.g. Mandurah, in a `REGION_ANCHOR`-mapped region), placeholder nearby-chip repoints across all Flat pages (which also cross-links batch-2 pages to each other), and the repairs-perth hub grid. Without that script the published pages are link-orphans. Related crawlability gotcha found the same day: **`/case-studies` paginates client-side (`PAGE_SIZE = 12`)**, so only the first 12 cards' links were in the server-rendered HTML and the other 8 case studies had no crawlable inbound link from their hub. `components/sections/case-study/case-studies-filter-grid.tsx` now also renders an `sr-only` `<nav>` listing *every* case study, so all are discoverable regardless of the visible page. Any listing that paginates or filters client-side needs the same complete SSR link set.
- **Article body text supports Markdown links only** — `renderRichText` in `components/sections/blog/article-content.tsx` parses `[label](/path)` in `paragraph` and `list` blocks (not `checklist`). There is **no bold/italic parsing**: `**text**` renders as literal asterisks on the page.

### Icons

Content stores icons as **strings** (e.g. `"Wrench"`). `lib/icons.ts` maps the string to a Lucide component via `resolveIcon(name)` / `iconMap` (falls back to `Wrench`). When content references a new icon, add it to `iconMap` or it silently falls back. (`components/page/icons.ts` is a parallel map for the page-template section components.)

### UI primitives

`components/ui/*` are the low-level primitives (button, dialog, card, accordion, carousel, tabs, sheet, select, etc.). They are built on **Base UI (`@base-ui/react`), not Radix** — there is no `@radix-ui` dependency. When adding or wiring a primitive, import from `@base-ui/react/...` and follow the existing files. `cn()` (clsx + tailwind-merge) lives in `lib/utils.ts` and is the standard className combiner. Styling is **Tailwind v4** (`app/globals.css`, no `tailwind.config`); a few components use v4-only syntax like `mask-[...]` and arbitrary `bg-[size:...]`.

### Interactive / conversion features (client components)

Beyond the static templates, several client-side widgets drive conversions. Most are mounted through the page templates and CTAs (`components/page/cta-buttons.tsx`, `components/sections/smart-cta.tsx`, `components/layout/sticky-mobile-cta.tsx`) and are dynamically imported with `ssr: false` to keep them off the critical path:

- **Smart price calculator** — `components/sections/smart-calculator/`. A two-mode widget behind one stable `SmartPriceCalculator` export (prop `showChatMode?: boolean`, default true): `index.tsx` (orchestrator + segmented **Calculator | Ask AI** toggle), `calculator-mode.tsx` (structured estimator as a **single-column step wizard** — 6 progressively-revealed steps in one internal scroll column; answering auto-scrolls *the calculator's own container* (never the page) to the next step via `data-step-id` anchors; the emergency toggle is a Standard/Emergency card pair; finishing reveals an inline estimate summary; a sticky bottom price bar shows on **every** breakpoint via `sticky-price-bar.tsx`, with `step-section.tsx` the step wrapper and the details sheet + summary reusing `price-panel.tsx`/`price-breakdown.tsx`), `chat-mode.tsx` (natural-language AI that reuses the assistant pipeline), `estimate-logic.ts` (pure engine), `constants.ts` (option lists **derived from the price list**), plus `option-card.tsx`/`suburb-input.tsx`. **`pricing-data.ts` is the single source of truth** for every service + scenario (exact Perth ranges, `perCount`/`perUnit` models, a customer-facing `publicNote` and a private LLM `internalNote`); it drives the calculator options, the estimate engine, **and** the CMS seed. **Pricing = table lookup + live DB override:** `calculateEstimate(form, catalog)` looks a scenario up by stable `id` (quantity-aware for springs/remotes/hinges), adds a flat **+$500 after-hours/emergency** surcharge when the toggle is on, then a matching live catalog row *overrides* the headline range (flagged "Live price"; matched by exact scenario name, then keyword). The catalog comes from the CMS via the same-origin proxy **`app/api/pricing/route.ts`** → `GET {CMS}/api/pricing-items` (anonymous, sanitised, `[]`-on-fail), module-cached by `use-pricing-catalog.ts` (`lib/cms/pricing-client.ts` is the typed server helper). Degrades to the baked ranges when the CMS is down. **Conversion CTAs (Get exact quote / Book / Call):** the "Get my exact quote" CTA opens `in-chat-quote.tsx`, which hosts the **live quote embed** pre-filled via URL params (`buildQuotePrefill` in `estimate-logic.ts` maps the selection to `{ suburb, serviceId }` — emergency→9, installation→2, maintenance→7, else repair 3; see **Quote embed** below). The **Ask AI** mode and the floating widget share `components/sections/chat/use-assistant-chat.ts` (messages/typing/`sendMessage` → `/api/chat`). Surfaced at the `app/calculator` route, via `components/sections/calculator-dialog.tsx`, and as the in-chat `calculator` overlay (`showChatMode={false}`). **Repopulate the CMS prices** with `npx tsx scripts/seed-pricing.ts` — re-runnable: logs into the admin API, deletes existing `PricingItems`, inserts `buildSeedRows()` (incl. the private `internalNote` the assistant reads and the +$500 emergency / quote-policy rows).
- **Booking dialog** — `components/sections/booking-dialog.tsx` embeds an **external live booking system in an iframe** (`https://booking-system-cgd.netlify.app/`). This is a separate app, not part of this repo. The same external app also serves the warranty register/check-status form, embedded full-page at `app/warranty-registration` via `components/sections/warranty/registration-frame.tsx` (`/warranty/register`).
- **Quote embed (real quote requests — no local forms)** — every "request a quote" surface renders the booking system's **live quote widget** (`https://booking-system-cgd.netlify.app/embed/quote`); submissions land directly in the CRM. `lib/booking-embed.ts` is the contract: the allowed prefill URL params (`firstName…postCode`, `serviceId`), `buildQuoteEmbedSrc`, `BOOKING_SERVICE_IDS` (live CRM catalog ids: repair 3, installation-quote 2, tune-up 7, emergency 9 — update if services are recreated in the CRM), and the `bookingServiceIdFor(text)` keyword mapper. `components/sections/quote-frame.tsx` renders the iframe (auto-resizes from the widget's `booking-widget-resize` postMessage; `fill` mode for dialogs/overlays); `quote-dialog.tsx` is the BookingDialog-style sheet, opened by `GetQuoteButton` (`components/page/cta-buttons.tsx`, used by the home About section). The section wrappers (`components/forms/*quote-form.tsx`, `components/sections/{service,problem,cost-guide,comparison}/quote-form.tsx`, `chat/in-chat-quote.tsx`) keep their card shells + `#quote`/`#get-quote` anchors and pass per-page prefill. There is **no** quote-complete postMessage from the widget (verified in its bundle), so no in-chat confirmation/lead callback; UTM/gclid attribution is not forwardable (not in the allowed params).
- **AI chat widget (Smart Garage Assistant)** — `components/sections/ai-chat-widget.tsx` (+ `components/sections/chat/*`). **LLM-backed and structured.** `sendMessage` POSTs the conversation (plus a per-tab `sessionId`) to the same-origin proxy `app/api/chat/route.ts`, which forwards (with the `x-assistant-secret` shared secret) to the CMS `POST /api/assistant/chat`. The CMS returns a **structured envelope `{ reply, suggestions, actions }`** (not plain text): the model generates follow-up `suggestions` (chips) and typed `actions` rendered uniquely by `chat-actions.tsx` — `call` (tel:), `book`/`calculator`/`quote` (open **in-chat overlays** inside the sheet via `in-chat-booking.tsx` / the `SmartPriceCalculator` / `in-chat-quote.tsx`), `suburb` (prefill+send), `link` (internal nav). **Hybrid RAG:** the CMS pre-retrieves cheap context and the model may emit JSON `queries` to pull precise pricing/services/suburbs/FAQs from the DB before answering (pricing incl. private `InternalNote`, never returned). The in-chat booking iframe listens for a `cgd:booking-complete` postMessage to auto-close (the booking app doesn't send it yet — manual "Back to chat" is the fallback). **Conversation logging + lead capture:** best-effort, the CMS upserts one `AssistantConversation` row per `sessionId` (transcript = client-facing turns only); admin views them at `app/admin/(dashboard)/conversations`. The `quote` action now opens the live quote embed (leads go straight to the booking CRM); `app/api/chat/lead` → CMS `POST /api/assistant/lead` still exists but currently has no frontend callers. (Requires the `AssistantConversations` table/migration in the CMS.) The DeepSeek key lives **only** in the CMS (`DeepSeek__ApiKey`), never in this repo; `deepseek-v4-flash` is a reasoning model (read only `content`, never `reasoning_content`). Non-streaming; the widget shows a typing indicator and falls back to a "call us" message on error. The conversation logic is shared with the price calculator's **Ask AI** mode via `components/sections/chat/use-assistant-chat.ts` (see Smart price calculator below). (The calculator's *estimate* is still deterministic — `estimate-logic.ts` — but now reads live DB pricing as an override, and its Ask-AI tab is the same DeepSeek+RAG pipeline as this widget.)
- **Sticky CTA + desktop chat launcher** — `components/layout/sticky-mobile-cta.tsx`. Mobile/tablet: a full-width Call + Smart Chat bar (revealed on scroll). Desktop (`lg+`): a single round chat-only bubble pinned bottom-right. Both triggers open one shared `AiChatWidget` instance. Rendered once per page by each page/template that wants it (hero, listing pages, the page templates, and `app/lp/layout.tsx`) — **not** by `SiteChrome`.

### Forms

There are **no local lead forms or server actions** anymore — the old `submitQuote` stub (`lib/actions/quote.ts`) and its form UIs were replaced by the live quote embed (see **Quote embed** above). `components/forms/*` are now thin wrappers around `QuoteFrame` with per-page prefill.

The business email is **never rendered as plaintext** (anti-spam-harvester): every "email us" surface uses `components/ui/obfuscated-email.tsx` (`ObfuscatedEmail`), which base64-decodes the address in `useEffect` so it exists only in the browser DOM, not the served HTML. `email` is deliberately omitted from all JSON-LD (`lib/seo/schema.ts`) for the same reason — keep the address out of `config/site.ts`-rendered output. If the email changes, update both `siteConfig.business.email` and the `ENCODED` constant in the component.

### Analytics & conversion tracking

**`lib/analytics.ts` `track(event, params)` is the only way this site emits an analytics event.** Added 2026-08-02, after a GA4 query found the property held *nothing but automatic events* (`page_view`, `session_start`, `scroll`, `first_visit`, `user_engagement`) — no call, quote, booking, chat or calculator signal existed, so there was no way to answer "which pages produce leads?".

`track()` pushes **both** shapes on purpose:
- `dataLayer.push({ event, ...params })` — a plain object, which is what **Google Tag Manager** reads. `gtag.js` ignores plain objects, so it is inert today and goes live the day a GTM container is added, **with no code change**. (Ads is planned, hence this shape.)
- `gtag("event", …)` pushed as an `arguments` object — which is what actually delivers to **GA4 today, without GTM**.

**This dual push is the whole point.** The pre-existing `trackChatEvent` did only the first, so every chat CTA event it fired for months was silently discarded. It now delegates to `track()`.

| Event | Fired from | Notes |
|---|---|---|
| `call_click` | `components/analytics/interaction-tracking.tsx` | **one delegated `document` listener**, not per-component — `tel:` links live in 20+ components plus CMS content, so per-call-site wiring guarantees drift. Matching on `href` also survives restyling, which a GTM CSS-selector trigger would not (Tailwind class strings change). |
| `email_click` | same listener | `mailto:` from `ObfuscatedEmail`. |
| `quote_open` | `quote-dialog.tsx` | intent only — see limitation below. |
| `booking_open` | `booking-dialog.tsx` | intent only. |
| `chat_open` | `ai-chat-widget.tsx` | effect on the `open` prop, so all three triggers are covered by one place. |
| `chat_message` | `chat/use-assistant-chat.ts` | carries `turn`. |
| `calculator_complete` | `smart-calculator/calculator-mode.tsx` | carries service/problem/price and `price_source` (whether the live CMS catalog or the baked fallback drove the range). |

**Gotcha the dialogs encode:** `QuoteDialog`/`BookingDialog` are usually driven by a parent setting `open` (GetQuoteButton, BookNowButton, the hero, the in-chat overlays), and Base UI does **not** call `onOpenChange` for externally-controlled opens. Tracking inside `handleOpenChange` alone therefore misses nearly every real open — verified in a browser, it fired zero events. Both dialogs now track from an effect on `open` *and* from `handleOpenChange` (for uncontrolled `trigger` use), with a ref keeping it to one event per open.

**What this deliberately cannot measure** — do not promise otherwise: the quote/booking widgets are a third-party cross-origin iframe that emits **no completion message** (verified in its bundle, see `lib/booking-embed.ts`), so `quote_open`/`booking_open` record intent and submissions exist only in the booking CRM. Phone calls placed by *reading* the number rather than tapping it are invisible to any client-side analytics.

`InteractionTracking` is mounted in `app/layout.tsx` **outside** the production-only analytics gate so events can be verified in dev (`track()` is harmless when gtag never loads — entries just sit unread in the array).

### Motion & performance (sensitive)

`framer-motion` is loaded lazily through `LazyMotionProvider`; GSAP + Lenis power the smooth-scroll and the garage-door intro/reveal animations. These areas are performance-tuned — consult the auto-memory notes (first-load performance, ScrollDoorReveal rendering) before touching the intro, scroll, or canvas-reveal code. `next.config.ts` enables `experimental.inlineCss` (prod only) and restricts image `qualities` to `[60, 75]`.

### CMS (live — ASP.NET Core + SQL Server)

The CMS that `docs/cms-architecture.md` designed is **built and running**, not a draft. It's a separate ASP.NET Core (clean-architecture) backend in **its own repo** at `C:\Users\Mohammad swedan\source\repos\Capital Garage Door CMS\` (API project `CapitalGarageDoor.Cms.Api`), serving `http://localhost:5179` in dev (`CMS_API_URL` overrides). In Development it auto-applies EF migrations and seeds the admin account against SQL Server **LocalDB** on startup — run it with `dotnet run --project "CapitalGarageDoor.Cms.Api" --launch-profile http`. Its 8 anonymous public GETs are **output-cached server-side** (5-min TTL backstop; freshness is event-driven — every content mutation evicts the matching cache tags post-commit via `PostCommitCacheBehavior`, which also now fires the revalidation webhook AFTER the DB commit, evict-first/notify-second; kill switch `OutputCache__Enabled=false` + restart). A cached response carries an `Age` header; requests with an `Authorization` header always bypass the cache.

**Frontend integration (this repo):**
- `lib/cms/*` — the typed API client (`client.ts`), admin/auth (`admin.ts`), reviews (`reviews-client.ts`), DTO→type mappers (`map-*.ts`), editor serializers, and content importers.
- `app/admin/*` — the CMS admin dashboard + page editor, plus `app/admin/api/*` route handlers that proxy the backend. `app/api/revalidate` is the ISR webhook the backend calls on **every page mutation that changes the public site** — create-as-published, update (both hrefs on a slug rename), publish, unpublish, delete. The webhook revalidates the page path, expires the `cms-sitemap` + `cms-page:{routeGroup}:{slug}` fetch tags (`revalidateTag(tag, { expire: 0 })` — Next 16 two-arg form), and refreshes `/sitemap.xml`, `/image-sitemap.xml`, `/feed.xml`, and the route group's listing page(s) — so sitemap + listings track CMS adds/deletes within seconds. `app/robots.ts` advertises both sitemaps and deliberately does **not** disallow `/lp/*` (noindex must stay crawlable to be seen).
- `app/api/chat/route.ts` — same-origin proxy for the **Smart Garage Assistant**. Forwards conversations to the CMS `POST /api/assistant/chat` (DeepSeek LLM + RAG over CMS content) with the `ASSISTANT_PROXY_SECRET` shared secret (= backend `Assistant:ProxySecret`). The DeepSeek key is **CMS-side only** (`DeepSeek__ApiKey`); it is never in this repo or the client bundle. See the AI chat widget under *Interactive / conversion features*.
- **Media library**: every image is a backend `Asset` carrying an `AssetCategory` (enum; FE option list `components/admin/asset-categories.ts`). `/admin/media` (`components/admin/media/*`) browses the library by category, with search + pagination, and edits alt/category or deletes (in-use assets are protected by the backend). The editor's **Asset Picker** (`components/admin/editor/asset-picker.tsx`) and the classic forms' image field (`AssetUploadField` in `components/admin/fields.tsx`) both offer **pick-from-library or upload** — every upload lands in the categorised library (shared `components/admin/media/asset-upload-form.tsx`). Backend `AssetsController` does category/alt-text filtering on the paged list plus `PUT`/`DELETE /api/admin/assets/{id}` (mutations via `app/admin/asset-actions.ts`).
- **Public Gallery vs Media Library** — two *separate* things. The media library (above) is the reusable asset store; the **public gallery** (`/gallery`) is a curated showcase that *references* library assets but is its own `GalleryItem` collection with its own metadata: `Title`, `ServiceType` (free-text — the public "type" filter), `Suburb` (optional), `Caption` (description/body), `Category`, optional before-image, `SortOrder`. Managed at `/admin/gallery` (`components/admin/gallery-*`, `app/admin/gallery-actions.ts`; picks its main/before images from the library via the Asset Picker). The public page (`components/sections/gallery/gallery-filter-grid.tsx`) filters by **type + category + suburb** and **paginates client-side** over the full ISR list (options/counts derived from the items); it reads through `getGalleryItems()` → `cmsGallery()` (`GET /api/gallery`, behind `CMS_CATALOGS`) with a `content/gallery.ts` fallback. Backend `GalleryController` (`api/admin/gallery` CRUD); the `Title`/`ServiceType`/`Suburb` columns were added by the `AddGalleryItemMetadata` migration. **Bulk-rebuild the gallery from a folder of real job photos** with `npx tsx scripts/seed-gallery.ts` (compresses to WebP → uploads to the Bunny zone `capital-garage-door/gallery/` → wipes the existing items → registers assets by CDN URL + creates items incl. before/after pairs → revalidates `/gallery`; needs `CMS_ADMIN_PASSWORD` + `REVALIDATE_SECRET`, and `BUNNY_STORAGE_KEY` or the CMS `appsettings.json` on disk). Its `MANIFEST` (per-photo SEO title/alt/caption/category) is the single source of truth and is re-exported to regenerate the `content/gallery.ts` fallback. To **ADD** photos without wiping the existing gallery, use `npx tsx scripts/add-gallery-images.ts` instead — same pipeline minus the delete loop, with a `suburb` field on each manifest entry (suburb-rich local-SEO metadata) and `sortOrder` continuing above the current max; it appends to `content/gallery.ts` rather than regenerating it. (Its July-2026 run added 20 suburb-tagged repair/roller/commercial/motor/before-after items — items 19–38.) **Gallery images MUST resolve to `jadara-hub.b-cdn.net`** (the only external host in `next.config.ts` `remotePatterns`) — a `GalleryItem` whose `Asset.CdnUrl` is on `localhost:5179` or `cgd.runasp.net` renders broken via `next/image` on the public page. `next/image` also carries a `title` attr on the gallery card + service/article/page heroes for image SEO; the image sitemap (`app/image-sitemap.xml`) additionally lists the home page's static images.
- The `lib/data/*.ts` cutover seam is **active**: per-type flags (`CMS_SERVICE_PAGES`, `CMS_COMPARISON_PAGES`, …, `CMS_CATALOGS`, `CMS_REVIEWS`) decide whether each type reads from the CMS or falls back to local `content/*.ts`. They now **default ON in code** (`(process.env.CMS_… ?? "on") === "on"`), so a deploy with no env set (e.g. Netlify) reads from the CMS; set a flag to `"off"` to force that type back to local content. Likewise `CMS_API_URL` **defaults to the deployed backend `https://cgd.runasp.net`** — `.env.local` overrides it to `http://localhost:5179` for local dev. Because production is the default, `npm run build` in any environment (not just with a local CMS) reaches the live CMS unless overridden — see **Build/runtime dependency** below.

**Build/runtime dependency:** `app/[slug]` resolution (`lib/cms/client.ts` `cmsResolve()`) **throws** when the API is unreachable, so `npm run build` — which prerenders those pages — **requires the backend running**, or it fails at static generation with `ECONNREFUSED`. The strict `cmsSitemap()` used by `app/sitemap.ts` also throws on failure (deliberate: at runtime a failed regeneration keeps serving the last good sitemap instead of a 200 that silently dropped every CMS URL), while the lenient `cmsSitemapSafe()` behind every `get…Slugs()` returns `[]`. Catalog/review reads fall back gracefully (`try/catch → []`), so non-`[slug]` routes (incl. the home page) still render without it. For a backend-free type check, use `npx tsc --noEmit`. `docs/admin-dashboard-and-editor-plan.md` covers the admin/editor build.

### Deployment (production)

**`https://capitalgaragedoors.com.au` serves from the user's own VPS** (Ubuntu 24.04, srv833288) as the isolated Docker Compose stack **`capitalgd`** — since 2026-07-06; design + runbook in `docs/superpowers/{specs,plans}/2026-07-06-vps-frontend-deployment*.md`. **Pushing to `main` is a production deploy**: `.github/workflows/deploy.yml` SSHes to the VPS, hard-resets `/opt/capitalgd`, writes `.env` from GitHub repo secrets (`REVALIDATE_SECRET`, `ASSISTANT_PROXY_SECRET` — must equal the CMS production values; source of truth is the CMS repo's gitignored `appsettings.Production.json`), and runs `docker compose up -d --build`. The `Dockerfile` builds with `DOCKER_BUILD=1`, which gates `output: "standalone"` (+ a sharp tracing include) in `next.config.ts` so the Netlify build is untouched; the image build prerenders against the live CMS (a failed build leaves the old container serving). Topology: Cloudflare (proxied, SSL mode **Full**, Always Use HTTPS) → host nginx `:80`/`:443` (`/etc/nginx/sites-available/capitalgd.conf`, self-signed origin cert in `/etc/nginx/certs/`, `www`→apex 301, **no** `/api` split — this app's `/api/*` routes must reach the container) → `127.0.0.1:8082` → `capitalgd_frontend` (port 3000). The VPS hosts two other untouchable stacks (`ecampus`, `alzoubi`); this repo is public, so **no secrets or the origin IP in committed files**. **Netlify (cgdperth.netlify.app) still builds `main` as a warm standby** — rollback = revert the two Cloudflare A records for apex+`www` to Netlify's `75.2.60.5`, proxy OFF.

## Keeping this file current

**Update CLAUDE.md whenever you make a major change to the system.** That includes: adding or removing a subsystem (e.g. the CMS, a new external service), an architectural shift, a new build- or runtime dependency, route-structure changes, or anything that would make a description above wrong. A stale CLAUDE.md silently misleads every future session — the "CMS (not built)" section was wrong for weeks because this rule wasn't followed. Fix the affected section in the **same change** that introduces the shift, not later.

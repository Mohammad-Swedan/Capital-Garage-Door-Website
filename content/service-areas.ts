import type { ServiceArea } from "@/types";

/**
 * Placeholder service areas. Replace with the actual cities/towns served
 * before launch — each entry generates a programmatic local-SEO landing page
 * at /service-areas/[slug].
 */
export const serviceAreas: ServiceArea[] = [
  {
    slug: "example-city",
    city: "Example City",
    state: "Example State",
    stateAbbr: "ST",
    description:
      "Capital Garage Door proudly serves Example City with fast, reliable garage door repair and installation.",
  },
];
